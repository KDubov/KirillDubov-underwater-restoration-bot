import os

BOT_TOKEN = os.getenv("BOT_TOKEN")

# HuggingFace Space URL (ВАЖНО: без https://huggingface.co/)
HF_SPACE = os.getenv("HF_SPACE")

# если есть токен (не всегда нужен)
HF_TOKEN = os.getenv("HF_TOKEN")
